export class Person {
    constructor(public id: string, public lastName: string, public type: string,
        public phone:string, public email:string, public picture: string, public status:string){}
}